# Agent 工作流 — 使用者接入指南

> 5 分钟看完，15 分钟跑通。复制即用，不需要理解设计原理。

---

## 0. 一句话说明

用 **PLAN.md**（任务队队列）和 **BUG-BOARD.md**（Bug 队列）作为 Agent 之间的通信管道，你做调度和审查，Agent 做执行。

**所有场景通用建议：** 在项目根目录创建 `.editorconfig` 文件，统一代码格式标准，减少 Agent 产生无意义的格式修改。内容如下：

```ini
root = true

[*]
indent_style = space
indent_size = 4
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.{cs,csproj,json,xml}]
indent_size = 4

[*.{js,ts,css,html}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false
```

---

## 1. 你现在是什么情况？（30 秒选场景）

```
你的项目是什么状态？
│
├─→ 什么都没有，从需求开始
│     └─→ 场景 A：新项目全周期
│
├─→ 有代码，不是我写的 / 我不熟
│     ├─→ 没上线 → 场景 B：接手未上线项目
│     └─→ 已上线 → 场景 B 变体
│
├─→ 有代码，是我要加新功能
│     └─→ 场景 C：迭代开发
│
└─→ 有代码，测试出 bug 要修
      └─→ 场景 D：测试修复
```

---

## 2. 接入步骤（按你选的场景）

### 场景 A：新项目全周期

**Step 1：建文件**

在项目根目录（和 .sln 同级）创建 `PLAN.md`：

```markdown
# 项目计划

## 当前阶段
规划期

## 需求列表
- [ ] 需求 1：
- [ ] 需求 2：

## 模块拆分
<!-- 每个模块分配给一个 Agent -->

## 接口契约
<!-- 前后端约定的 API 格式 -->
```

创建 `BUG-BOARD.md`：

```markdown
# Bug 看板

## 待修复 (TODO)

## 修复中 (IN PROGRESS)

## 已修复待验证 (DONE)
```

**Step 2：开 Agent 窗口**

在 ZCode 中开两个对话窗口。

前端窗口发送：
```
你是我的前端开发 Agent。你的职责是开发前端功能。

工作规则：
1. 你只修改前端相关文件（.cshtml, .razor, .js, .css 等）
2. 不要修改后端代码
3. 每次只处理一个任务，完成后更新 PLAN.md 状态
4. 发现问题根源在后端，写入 BUG-BOARD.md 标注【后端】
5. 开始前先读 PLAN.md 了解项目背景
6. 重要：不要修改与当前任务无关的文件，不要做格式化、空格调整、编码转换等无意义修改，只改动必须改的代码，保持其他内容原样

项目路径：[你的项目路径]
```

后端窗口发送：
```
你是我的后端开发 Agent。你的职责是开发后端功能。

工作规则：
1. 你只修改后端相关文件（.cs, .csproj, .sql 等）
2. 不要修改前端代码
3. 每次只处理一个任务，完成后更新 PLAN.md 状态
4. 开发完跑一下单元测试确认没问题
5. 开始前先读 PLAN.md 了解项目背景
6. 重要：不要修改与当前任务无关的文件，不要做格式化、空格调整、编码转换等无意义修改，只改动必须改的代码，保持其他内容原样

项目路径：[你的项目路径]
```

**Step 3：开始工作**

1. 在 PLAN.md 里写清楚第一个要做的功能
2. 切到对应窗口："读 PLAN.md，开发 [任务名称]"
3. Agent 做完后你验证，通过就打勾
4. 发现 bug 走场景 D 的流程

---

### 场景 B：中途接手项目

**Step 1：建文件**（同场景 A，建 PLAN.md 和 BUG-BOARD.md）

**Step 2：让 Agent 探索代码**

在任一 Agent 窗口发送：
```
你刚接手一个项目，项目路径是 [路径]。
请帮我了解这个项目的架构：
1. 读一下项目结构和主要文件
2. 输出：项目架构概览、关键文件列表、技术栈
3. 把结果保存到 PLAN.md 的开头部分
```

**Step 3：明确任务**

探索完成后，根据你要做的事切换到场景 C 或 D。

---

### 场景 C：迭代开发

**Step 1：建文件**

`PLAN.md`：
```markdown
# 项目计划

## 当前迭代目标
<!-- 本次迭代要完成什么 -->

## 任务列表
- [ ] 任务 1：
- [ ] 任务 2：

## 接口契约
```

`BUG-BOARD.md`：（同上）

**Step 2：开发**

1. 更新 PLAN.md，写清任务
2. 切到对应 Agent 窗口："读 PLAN.md，开发 [任务名称]"
3. Agent 做完后验证
4. 发现 bug 走 BUG-BOARD 流程

---

### 场景 D：测试修复

**Step 1：建文件**

`PLAN.md`：
```markdown
# 项目计划

## 当前阶段
测试与 Bug 修复

## 测试进度
- [ ] 模块 A
- [ ] 模块 B
```

`BUG-BOARD.md`：（同上）

**Step 2：开窗口**

前端窗口：
```
你是我的前端开发 Agent。你的职责是修复前端 bug。

工作规则：
1. 只修改前端文件
2. 每次只修一个 bug，修完更新 BUG-BOARD.md 状态
3. 发现根因在后端，写入 TODO 标注【后端】
4. 开始前先读 PLAN.md 和 BUG-BOARD.md
5. 重要：不要修改与当前任务无关的文件，不要做格式化、空格调整、编码转换等无意义修改，只改动必须改的代码，保持其他内容原样

项目路径：[你的项目路径]
```

后端窗口：
```
你是我的后端开发 Agent。你的职责是修复后端 bug。

工作规则：
1. 只修改后端文件
2. 每次只修一个 bug，修完更新 BUG-BOARD.md 状态
3. 修复后跑单元测试确认没问题
4. 发现根因在前端，写入 TODO 标注【前端】
5. 开始前先读 PLAN.md 和 BUG-BOARD.md
6. 重要：不要修改与当前任务无关的文件，不要做格式化、空格调整、编码转换等无意义修改，只改动必须改的代码，保持其他内容原样

项目路径：[你的项目路径]
```

**Step 3：测试修复循环**

```
你测试 → 发现 bug → 写入 BUG-BOARD.md → 派给 Agent → Agent 修 → 你验证
         ↑                                                          │
         └──────────── 通过则清除条目，继续下一个 ←──────────────────┘
```

---

## 3. 日常操作速查卡

```
┌──────────────────────────────────────────────────┐
│              Agent 工作流快速参考                   │
├──────────────────────────────────────────────────┤
│                                                  │
│  发现 Bug  → 写入 BUG-BOARD.md（TODO）           │
│  分配前端  → 前端窗口："看 BUG-BOARD.md，修前端的" │
│  分配后端  → 后端窗口："看 BUG-BOARD.md，修后端的" │
│  验证通过  → 清掉条目                             │
│  验证失败  → 补充原因，打回重修                    │
│  回归测试  → "跑全部测试，确认没引入新问题"         │
│  Agent 要了解全局 → "读 PLAN.md 和 BUG-BOARD.md"  │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## 4. 常见场景 Prompt 速查

| 场景 | 复制这段话到 Agent 窗口 |
|---|---|
| 分配 Bug 修复 | `读 BUG-BOARD.md 的 TODO 列表，修复标注了【前端/后端】的那条。修完更新状态到 DONE。` |
| 跑回归测试 | `跑一下项目的全部测试用例（dotnet test）。如果有失败的，分析哪些是新引入的。` |
| 代码审查 | `请审查 [文件路径] 的最近修改，重点关注：1. 是否过度复杂 2. 异常处理 3. 安全性 4. .NET 最佳实践` |
| 总结进展 | `读 PLAN.md 和 BUG-BOARD.md，给我一个简洁的状态汇报。` |
| 修复引入新 Bug | `你刚才的修复似乎引入了新问题：[描述]。请分析是否是你的修复导致的。` |
| Agent 了解全局 | `读一下 PLAN.md 和 BUG-BOARD.md，了解项目当前状态。` |
| 指定修某个 Bug | `修复 BUG-BOARD.md 中 [bug 标题]，修完跑测试确认。` |
| 并行修多个 Bug | `读 BUG-BOARD.md 的 TODO，修复所有标注了【后端】的条目，每修完一个更新状态。` |

---

## 5. 出问题了？

### Agent 修错了
在对应窗口说："你刚才的修改有问题，[描述]，请撤回并重新修复。"

### 两个 Agent 改同一个文件
不要并行，按顺序来。在 BUG-BOARD.md 里标注"与 xxx 有文件冲突"。

### Agent 不知道该做什么
让它先读文件："读一下 PLAN.md 和 BUG-BOARD.md，了解当前状态。"

### 不确定 Bug 是前端还是后端
先派给更可能是根因的那一方。比如接口返回格式问题，先派后端。

### 项目阶段变了（比如从测试进入新功能开发）
更新 PLAN.md 的"当前阶段"和任务列表，BUG-BOARD.md 继续用于修 bug。

---

## 6. 文件维护规则

| 文件 | 什么时候更新 |
|---|---|
| BUG-BOARD.md | 发现 bug 立刻写入 TODO；Agent 修完移到 DONE；验证通过清除 |
| PLAN.md | 早上更新任务列表；任务完成后打勾；接口变了更新契约部分 |

**唯一原则：文件是唯一的真相来源。不要在聊天里描述应该写在文件里的东西。**

---

## 7. 防止 Agent 无意义修改

Agent 修改文件时经常会"顺手"调整空格、换行、编码格式，产生无意义的 git diff。三层防护：

### 第一层：Agent Prompt（已内置）

所有 Agent 初始化 prompt 已包含规则：
```
重要：不要修改与当前任务无关的文件，不要做格式化、空格调整、编码转换等无意义修改，只改动必须改的代码，保持其他内容原样。
```

### 第二层：.editorconfig（推荐）

在项目根目录创建 `.editorconfig`（内容见第 0 节），统一格式标准。

### 第三层：Git Pre-commit Hook（可选）

在 `.git/hooks/pre-commit` 创建脚本，自动丢弃纯空格修改：

```bash
#!/bin/bash
changed_files=$(git diff --cached --name-only --diff-filter=M)
for file in $changed_files; do
    if git diff --cached --ignore-all-space --ignore-blank-lines --quiet -- "$file" 2>/dev/null; then
        echo "⚠️  跳过纯空格修改: $file"
        git reset HEAD -- "$file"
        git checkout -- "$file"
    fi
done
```

执行 `chmod +x .git/hooks/pre-commit` 赋予权限。

### 已有纯空格修改？一行命令修复

```bash
git diff --ignore-all-space --ignore-blank-lines --name-only | xargs git checkout --
```
